<h1>Listing-Feature Entity</h1>

<p>Use the form below to add a listing-feature to the database!</p>

<form id="addRecord">
    <fieldset>

        <label for="feature">Feature:</label><br>
        <input class="addRecord" type="text" name="feature" id="feature"><br>
		<button type="button" name="Submit" id="addRecordButton">Submit</button>
   	
    </fieldset>
</form><br>

<p>The table below shows the records you have stored in the database.</p>

<table id="listingFeatureTable">
    <tr>
        <th>Name</th>
    </tr>
    
    {{#if results}}
        {{#each results}}
            <tr>
                <td>{{this.feature}}</td>
				<td><a href="/editRecord?id={{this.id}}"><input type="button" value="Update"></a></td>
                <td>
                    <input type="button" name="delete" value="Delete" onClick="deleteData('listingTable',{{this.id}})">
                    <input type="hidden" id="delete{{this.id}}">
                </td>
            </tr>
        {{/each}}
    {{/if}}
</table>

<script src="public/db.js"></script>
